---
# Helm version to install
helm_version: "v3.19.0"

# Installation directory
helm_install_dir: "/usr/local/bin"

# Download settings
helm_os: "linux"

# Temporary location
helm_tmp_dir: "/tmp/helm-install"

# Architecture mapping
helm_arch_map:
  x86_64: amd64
  amd64: amd64
  aarch64: arm64
  arm64: arm64

helm_checksum_value: "a7f81ce08007091b86d8bd696eb4d86b8d0f2e1b9f6c714be62f82f96a594496"